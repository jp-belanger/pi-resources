{
  "name": "pi-resources",
  "version": "0.1.0",
  "private": true,
  "description": "Personal Pi extensions and prompt templates package.",
  "keywords": [
    "pi-package"
  ],
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "format": "biome format --write extensions",
    "lint": "biome lint extensions",
    "check": "biome check extensions"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ],
    "prompts": [
      "./prompts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.4"
  }
}
